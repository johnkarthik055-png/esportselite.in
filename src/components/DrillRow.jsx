/**
 * DrillRow.jsx — backward-compat re-export.
 *
 * All timer logic, Firestore writes, and the same-tab sync fix now live in
 * DrillTimer.jsx. ModuleCard continues to import DrillRow without change.
 */
export { default } from './DrillTimer.jsx'
